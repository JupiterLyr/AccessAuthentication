# Changelog

All notable changes to this project will be documented in this file.

The format is based on [Keep a Changelog](https://keepachangelog.com/en/1.1.0/),
and this project adheres to [Semantic Versioning](https://semver.org/spec/v2.0.0.html).</br>

---

`AccessAuthentication.exe`: main software (主软件)
`config_gen.exe`: sub-software, which is called configurations generator (配置生成器)
**Note:** Please understand that the changelog is written in Chinese for personal reference!

---

## [2.2.2]
### Changed
- 为确保规范性，将 `encoder.h` 改为 `encoder.hpp`，并同步修改了关联的引用

## [2.2.1]
### Fixed
- 去除了 `report` 提示弹窗中的按钮，优化了其显示逻辑
- 修订了帮助手册的内容
### Deprecated
- 手动设置文字语言将逐步过渡到切换语言
- 消息弹窗的双语显示可能会被自动语言取缔
### Issue
1. 在未来的版本中，可能进一步优化进度条的显示内容和 UI
2. 下一步将会优化多语言问题，减少对话框中的干扰信息
3. 为提升用户体验，后续版本可能会针对配置生成器进行升级，可能变成基于 UI 的管理工具。考虑工程量较大，此工期可能较长，敬请期待！

## [2.2.0]
采用多线程及其错误判别机制的**稳定运行版**！
### Added
- 创建了 `CHANGELOG.md` 以规范化记录版本更迭情况
- 封装了 `tasks.cpp` 中所有可能的错误码及其说明字符串，见 `error_code.h` 和 `error_code.cpp`
### Fixed
- 修复了 `protect` 过程无法取消的 bug
- `error_code.cpp` 中的文本已嵌套多语言模块 `tr()` 以提前适配未来版本
- 完善了错误码判别机制

## [2.1.0-pre]
**重要更新**！完善了多线程的管理机制，规范化了各种错误、校验、警告、中断等返回值，加入了进度条，但尚未配置各种返回值的后续操作，当前版本**仅供预览功能**！
### Added
- 添加了 `TaskBase` 基类，便于获取当前任务以精细化管理
- 添加了 `normalizeDbFile()` 函数，在未成功删除 DB 文件时，用户可以直接手动删除 DB 文件，以免造成额外残留
### Fixed
- 利用 `enum` 增设了规范化管理的返回值 `TaskResult`，为后续管理返回值提供便利
- 调整了部分 UI，将 `Confirm` 和 `Cancel` 改为 `Unlock` 和 `Close`，并在点击 `Protect` 或 `Unlock` 时暂时禁用两按钮，直到相关操作完成
- 优化了信号-槽机制，加入了百分比进度条，实时显示运行进度，但进度条数据显示还有进一步优化空间

## [2.0.0-alpha]
原版本号 `v1.6.1-alpha`，因版本号规范性要求及接口不兼容等问题，更改为 `v2.x.x`。该版本无法保证安全性、有效性和稳定性，**切勿使用当前版本**！
### Removed
- 彻底删除了 `locker.cpp` 和 `locker.h`
### Fixed
- 优化了多线程机制和相关架构
- 重写了 `tasks.cpp` 中的 `folder2db` 与 `db2folder` 的实现逻辑，采用分块读写的形式，减少单次内存消耗量
- 调整了部分 signal

## [1.5.4-beta]
该版本对架构进行了大规模调整。
### Added
- 引入了多线程机制和线程池，增设了用于管理线程的 `processor` 模块和用于实施子线程任务的 `tasks` 模块
### Deprecated
- 因架构调整，`locker` 模块可能冗余
### Fixed
- 对架构进行了优化，将 `locker` 模块的函数封装进 `tasks` 模块的类中

## [1.5.0]
### Fixed
- 在主软件和配置生成器之间使用 IPC 实现进程通知，已将相关配置写入 `main.cpp`
- 运行配置生成器时，若检测到主软件正在运行，则提示用户结束主软件

## [1.4.4]
**首个正确运行版本**！但仍有部分问题：遇到大型文件可能会界面卡死，未来版本将修复此问题。
### Added
- 提醒用户标识符的唯一性与密码构成的合法性
- 主软件中加入了帮助按钮（图标为问号），使用 `help.md` 传递使用说明
- 为配置生成器也接入了使用说明
- 设置配置生成器的弹窗等待时长为 3 秒
### Fixed
- 修复了标识符重复时引发的 bug；现在写入配置时，若侦测到重复的标识符，则直接取消配置
- 将原先基于 `QMessageBox` 的对话框改为 `QDialog`，以便进行自定义对话框操作

## [1.4.1-beta]
现已支持解读以 `Z:\` 开头的路径，将重定向至软件运行位置所在盘符。该功能仅在 Windows 系统上测试通过，其余系统尚未测试。</br>
此次更新旨在解决可移动存储媒介在 Windows 系统中盘符可变的问题。实际上，当用户在配置跳转路径时，若使用 `Z:\` 作为盘符，则表示待跳转的文件夹与该软件运行路径所在盘符相同。当用户把软件放在可移动存储媒介上时，相当于利用了这一特性。
### Added
- 解决了可移动存储媒介的兼容和校验问题
- 生成 DB 文件后，若在 Windows 系统下运行，则将文件设置为隐藏属性和受系统保护属性，一般情况下其 DB 文件将不可见
### Removed
- 移除了配置生成器运行弹出窗口中的图标
### Fixed
- 优化了 `config_gen.cpp` 和 `mainwindow.cpp` 中的部分逻辑
- 当文件夹不存在时，现在不会错误地创建 DB 文件了
- 更新了软件说明文档，加入了更详细的使用说明

## [1.3.2]
### Added
- 引导用户的使用方法
- 预先加入了可移动存储媒介的盘符处理的解决策略说明
### Changed
- 将 `config_gen.cpp` 的手动输入路径改为弹出文件对话框选择文件，并优化了传递逻辑

## [1.3.0]
### Added
- 对原本的配置信息加入了混淆机制，防止配置文件内容泄漏
- 用 `encoder.txt` 记录了编码算法框架，并参与 Git 提交（不包含核心算法）
- 加入防误触机制，用户点击 `Cancel` 按钮后，需二次确认是否关闭
### Changed
- 原先的工具要求用户使用 CSV 文件作为辅助文件，用以写入配置；为了更加用户友好，现改为了 TXT 文件

## [1.2.0]
### Added
- 开发并部署了 `Protect` 功能，用于文件夹打包伪装，目前采用仅验证标识符的方式
### Changed
- 修改了验证通过后的实现逻辑
### Fixed
- 完善了打包、解包行为
- 将消息弹窗的内容规范化、合理化，不轻易显示文件夹位置

## [1.1.0]
当前版本**不可用**！仅包含验证模块，不包含加密模块，无法为您的文件提供保护！
### Added
- 配置了验证机制，可以在本地正常使用
### Deprecated
- 文件夹的安全性需要由用户自行配置，且使用难度大，后续版本将提出全新方法解决此问题
### Security
- 提高了 `encoder.h` 的加密算法复杂度

## [1.0.0-beta]
### Added
- 开发了信息验证模块，其中加密验证代码被保护在了未上传的 `encoder.h` 中，目前该方法正处于调试阶段
- 开放了 `config_gen.cpp` 的版本控制机制，其中核心加密算法移动到了 `encoder.h` 中

## [0.7.3-alpha]
此版本系开发内测版，是开发初期迭代的最后一个版本。该版本不具备任何实质性功能，仅完成了前端开发。以下按时间由近及远的顺序罗列了主要开发历程：
- 加入了首个用于生成 BIN 文件的**配置生成器**工具，其源文件放置于 `tools` 文件夹下，需要通过命令行进行配置
- 利用 QSS 优化了 UI 设计，并在 `main.cpp` 中调用 QSS，将主界面改为圆角，隐藏了系统窗口，使得界面看起来更加现代
- 将 `centralWidget` 作为总底板，`centralLayout` 用于界面居中，将 `mainWidget` 作为交互窗口，并在窗口后方添加了阴影
- 重写并优化了 `SlideButton`，提供了组件接口，能够丝滑地交互滑动按钮组件
- 用直接修改文字的方式加入了中英文切换功能